# Legacy Scripts

This directory holds archived joystick-control variants and older support scripts that are kept for reference.

The active control entry point for this experimental checkout is `../dorna_joy_control.py`.

Most files here were written to run from the repository root and are not the primary maintained path.
